# Copie este archivo a `.env` antes de levantar el despliegue:  cp .env.example .env

# --- Tier 3: base de datos (auth-service) ---
POSTGRES_USER_AUTH=auth
POSTGRES_PASSWORD_AUTH=auth
POSTGRES_DB_AUTH=authdb

# --- Tier 3: base de datos (products-service) ---
POSTGRES_USER_PRODUCTS=products
POSTGRES_PASSWORD_PRODUCTS=products
POSTGRES_DB_PRODUCTS=productsdb

# --- Tier 2: microservicio ---
JWT_SECRET=cambie-esta-clave-por-una-aleatoria-larga
JWT_EXPIRATION_MINUTES=30
# Taller 2, Requerimiento 9: mismo JWT_SECRET/JWT_ALGORITHM/JWT_ISSUER los usa
# `web` para decodificar localmente (ver docker-compose.yml, x-web-env).
# JWT_AUDIENCES lo usa auth-service al emitir el token (Requerimiento 9b).
JWT_ALGORITHM=HS256
JWT_ISSUER=auth-service
JWT_AUDIENCES=auth-service,products-service
MAX_FAILED_ATTEMPTS=5
LOCKOUT_SECONDS=300

# --- Tier 1: web (tácticas de disponibilidad del cliente) ---
AUTH_TIMEOUT_SECONDS=3
RETRY_ATTEMPTS=3
BREAKER_FAILURE_THRESHOLD=5
BREAKER_RECOVERY_SECONDS=15

# --- Observabilidad ---
LOG_LEVEL=INFO

# Alertmanager: envío de alertas por correo (Gmail).
# SMTP_PASSWORD debe ser una "contraseña de aplicación" de Google, no tu
# contraseña normal: https://myaccount.google.com/apppasswords
SMTP_USER=tu-correo@gmail.com
SMTP_PASSWORD=contraseña-de-aplicacion-de-16-caracteres
ALERT_EMAIL_TO=tu-correo@example.com

# --- pgAdmin: administración de base de datos ---
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
